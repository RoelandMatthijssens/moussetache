= form_for pricerow do |f|
	= f.hidden_field :product_id, :value => product.id
	%tr
		%td= f.text_field :description
		%td= f.text_field :currency
		%td= f.text_field :value
		%td= f.text_field :affix
		%td= f.submit 'Save'
